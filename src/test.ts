import {Experiment, Variant} from './'

describe('Experiment', () => {
  it('is truthy', () => {
    expect(Experiment).toBeTruthy()
  })
})

describe('Variant', () => {
  it('is truthy', () => {
    expect(Variant).toBeTruthy()
  })
})
